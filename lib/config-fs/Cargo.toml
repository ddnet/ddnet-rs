[package]
name = "config-fs"
version = "0.1.0"
edition = "2021"

[dependencies]
base-io = { path = "../base-io" }
config = { path = "../config" }

anyhow = { version = "1.0.97", features = ["backtrace"] }

[package]
name = "auto-mapper"
version = "0.1.0"
edition = "2021"

[dependencies]
math = { path = "../../../lib/math" }
api = { path = "../../../lib/api" }

api-auto-mapper = { path = "../../../game/api-auto-mapper" }
editor-interface = { path = "../../../game/editor-interface" }

log = "0.4.28"
rustc-hash = "2.1.1"

[lib]
crate-type = ["cdylib"]
